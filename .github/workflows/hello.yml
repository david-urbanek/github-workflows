name: 'Hello World'
on:
  push:
    branches: [main]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: 'Say Hello'
        run: echo "Hello, World!"
      - name: 'Run date'
        run: date
      - name: 'Run ls -l'
        run: ls -l
